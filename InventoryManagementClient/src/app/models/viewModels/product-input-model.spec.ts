import { ProductInputModel } from './product-input-model';

describe('ProductInputModel', () => {
  it('should create an instance', () => {
    expect(new ProductInputModel()).toBeTruthy();
  });
});
