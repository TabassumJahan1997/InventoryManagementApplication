import { OrderItemViewModel } from './order-item-view-model';

describe('OrderItemViewModel', () => {
  it('should create an instance', () => {
    expect(new OrderItemViewModel()).toBeTruthy();
  });
});
