import { CustomerViewModel } from './customer-view-model';

describe('CustomerViewModel', () => {
  it('should create an instance', () => {
    expect(new CustomerViewModel()).toBeTruthy();
  });
});
